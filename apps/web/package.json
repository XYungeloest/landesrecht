{
  "name": "@landesrecht/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler deploy --config dist/server/wrangler.json --env=\"\""
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.1",
    "@landesrecht/legal-core": "^0.1.0",
    "@landesrecht/providers": "^0.1.0",
    "@landesrecht/runtime": "^0.1.0",
    "@landesrecht/search": "^0.1.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "wrangler": "^4.131.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
